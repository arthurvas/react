import R from 'ramda'

export const getUserById = (state, id) => R.prop(id, state.users)

export const getUsers = state => {
    const users = R.map(id => getUserById(state, id), state.usersPage.ids)
    return users
}